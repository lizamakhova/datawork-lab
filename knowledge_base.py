# База знаний DataWork Lab
# knowledge_base.py
KNOWLEDGE_BASE = {
    "partner_statuses": {
        "title": "📋 Статусы операций в реестрах партнеров",
        "content": """
        ## Партнер А
        - **COMPLETED** = успешная операция
        - **DECLINED** = отказанная операция  
        - **IN_PROGRESS** = операция в процессе

        ## Партнер Б
        - **SUCCESS** = успешная операция
        - **FAILED** = отказанная операция

        ### 💡 Важные правила:
        - Данные партнера всегда приоритетны над нашими
        - При расхождениях статусов исправляем НАШИ данные
        - Статусы партнера являются источником истины

        ### 🎯 Пример расхождения:
        - Наш статус: `success`
        - Статус партнера: `DECLINED`
        - Правильное действие: Исправить наш статус на `failed`
        """
    },
    "revenue_calculation": {
        "title": "🧮 Расчет выручки",
        "content": """
        ## Формула расчета выручки:
        ```
        Выручка = amount - commission_amount
        ```

        ## Таблица ставок комиссий:
        | Партнер | Процент | Фиксированная | Итоговая формула |
        |---------|---------|---------------|------------------|
        | PARTNER_A | 2% | 0.50 EUR | amount * 0.02 + 0.50 |
        | PARTNER_B | 1.5% | 1.00 EUR | amount * 0.015 + 1.00 |

        ## ⚠️ Процесс при расхождениях:
        1. **Найти расхождение** между расчетом и реестром
        2. **НЕ править реестр партнера**
        3. **Запросить корректирующий реестр** у партнера
        4. **Дождаться официального исправления**

        ### 🎯 Пример проверки выручки:
        ```sql
        SELECT p.processing_id, p.amount, p.commission_amount,
               (p.amount - p.commission_amount) as выручка
        FROM processing_operations p
        WHERE p.status = 'success'
        AND p.created_date = '2025-01-15'
        ```
        """
    },
    "dba_requests": {
        "title": "🛠️ Форматы запросов к DBA",
        "content": """
        ## 📝 Правильный формат запросов:

        ### ✅ UPDATE запросы:
        ```sql
        UPDATE таблица SET поле=значение WHERE условие
        ```
        **Пример:**
        ```sql
        UPDATE processing_operations 
        SET status='failed' 
        WHERE processing_id='PA023'
        ```

        ### ✅ INSERT запросы:
        ```sql
        INSERT INTO таблица (поле1, поле2) VALUES (знач1, знач2)
        ```
        **Пример:**
        ```sql
        INSERT INTO processing_operations 
        (processing_id, status, amount) 
        VALUES ('PA999', 'success', 500.00)
        ```

        ## 🚫 Запрещенные операции:
        - `DROP`, `DELETE`, `TRUNCATE` - не выполняются
        - Запросы без `WHERE` условия - отклоняются
        - Изменение партнерских данных - не допускается

        ## 🔒 Требования безопасности:
        - Всегда делай бэкап перед массовыми изменениями
        - Тестируй запросы на тестовых данных
        - Согласовывай опасные операции с руководителем

        ### ✅ Обязательно для бэкапа:
        Пример корректного запроса:
        ```sql
        CREATE TABLE processing_operations_backup_20251203 AS 
        SELECT * FROM processing_operations WHERE created_date = '2025-01-15'
        ```
        → DBA выполнит и бэкап, и основной запрос.

        > ℹ️ **Без отдельного запроса на бэкап** — изменения **не будут выполнены**.
        """
    },
    "data_quality": {
        "title": "🔍 Контроль качества данных", 
        "content": """
        ## 🎯 Типичные проблемы данных:

        ### 1. Расхождения статусов
        **Проблема:** Разные статусы у нас и у партнера  
        **Решение:** Привести наши статусы в соответствие с партнером

        ### 2. Неправильные комиссии
        **Проблема:** Комиссия в реестре не соответствует расчету  
        **Решение:** Запросить корректирующий реестр у партнера

        ### 3. Дублирующиеся реестры
        **Проблема:** Несколько активных реестров на одну дату  
        **Решение:** Уточнить у партнера какой реестр корректен

        ### 4. Отсутствие связей
        **Проблема:** Нет связи между нашими и партнерскими операциями  
        **Решение:** Проверить operation_additional_data на корректность связей

        ## 🛠️ Процесс исправления:

        ### Для НАШИХ данных:
        1. Найти проблему через SQL запрос
        2. Сделать бэкап данных
        3. Отправить запрос DBA на исправление
        4. Проверить результат

        ### Для данных ПАРТНЕРА:
        1. Найти расхождение
        2. Подготовить расчеты и доказательства
        3. Отправить запрос партнеру через соответствующий чат
        4. Дождаться корректирующего реестра

        ## 📊 Мониторинг качества:
        ```sql
        -- Проверка расхождений статусов
        SELECT COUNT(*) as discrepancies
        FROM processing_operations p
        JOIN operation_additional_data o ON p.processing_id = o.processing_id
        JOIN partner_a_payments a ON o.additional_value = a.partner_id
        WHERE (p.status = 'success' AND a.status != 'COMPLETED')
           OR (p.status = 'failed' AND a.status != 'DECLINED')

        -- Проверка дублей реестров
        SELECT partner_contract_id, registry_date, COUNT(*) as registry_count
        FROM registry_statuses 
        WHERE is_excluded = 0
        GROUP BY partner_contract_id, registry_date
        HAVING COUNT(*) > 1
        ```
        """
    },
    "task_report_template": {
        "title": "📋 Шаблон оформления задачи",
        "content": """
        В процессе выполнения любой задачи мы документируем все шаги и выводы.  
        Это может потребоваться для аудита, передачи контекста коллегам или обучения новых сотрудников.

        Заполняйте кратко, но **достаточно для воспроизведения**:
        
        #### 1. Описание проблемы  
        Укажите:  
        - Какая задача/метрика,  
        - За какой период,  
        - Чем выражено расхождение (цифры, статусы, кол-во записей).  
        Пример:  
        > *«Выручка за 15.01: 12 345.67 EUR (наша БД) vs 12 392.87 EUR (реестр партнёра А) — расхождение 47.20 EUR»*

        #### 2. Что правим  
        Опишите:  
        - Какие данные или процессы изменяете,  
        - В какой таблице/реестре,  
        - Конкретный SQL- или бизнес-запрос,  
        - Меры предосторожности (бэкап, тестирование).  
        Пример:  
        > *«Исправляем статус операции PA023: UPDATE processing_operations SET status = 'failed', commission_amount = 0 WHERE processing_id = 'PA023'. Перед этим создаём бэкап: CREATE TABLE po_backup_20251203 AS SELECT * FROM processing_operations WHERE processing_id = 'PA023'»*

        #### 3. Фактический результат  
        Приведите:  
        - Состояние **до** и **после** (цифры, статусы),  
        - Как проверили корректность (запрос, сверка с партнёром),  
        - Остаточное расхождение (если есть — объясните почему).  
        Пример:  
        > *«До: выручка 12 345.67 EUR. После: 12 392.87 EUR. Проверка: SELECT SUM(amount - commission_amount) = 12 392.87. Расхождение устранено»*

        > 💡 Совет: если сомневаетесь — представьте, что этот текст прочитает коллега через 3 месяца. Сможет ли он понять, что было, что сделано и почему?
        """
    }
}
